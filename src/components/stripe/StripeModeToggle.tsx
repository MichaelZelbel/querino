// NOTE: Stripe mode toggle is currently disabled. To re-enable,
// restore the toggle UI from git history.

interface StripeModeToggleProps {
  showInProduction?: boolean;
}

export function StripeModeToggle(_props: StripeModeToggleProps) {
  return null;
}
